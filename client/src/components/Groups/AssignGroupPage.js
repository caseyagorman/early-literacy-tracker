import React from "react";

const AssignGroupPage = props => (
  <div style={{ fontFamily: "krub" }}>
    <h1>Assign group.</h1>
    <br />
    <p>
      Hold down shift to select multiple adjacent students. Hold down command
      (mac) control (PC) <br />
      to select multiple non-adjacent students. <br /> Click group name to view
      group details.
    </p>
    <br />
    <br />
  </div>
);

export default AssignGroupPage;
